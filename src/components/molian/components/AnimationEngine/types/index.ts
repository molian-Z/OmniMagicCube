// 从@/types/animation导入并重新导出所需类型
import { AnimationConfig, AnimationEffect, ComponentAnimations } from '@/types/animation';

export type {
  AnimationConfig,
  AnimationEffect,
  ComponentAnimations
};

// 也可以在这里定义其他动画引擎特有的类型